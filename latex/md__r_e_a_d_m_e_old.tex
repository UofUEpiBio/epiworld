\hypertarget{md__r_e_a_d_m_e_old_autotoc_md49}{}\doxysection{Disease dynamics}\label{md__r_e_a_d_m_e_old_autotoc_md49}
Diseases continuously evolve in time. Changes in their genetic sequence make them more or less resistant to the particular version of the vaccine. Mutations also affect the transmissibility level and mortality rate of the disease. Using this approach allows making vaccination efficacy a function of compatibility between the variant and the vaccine.

When an individual becomes infected, the disease accumulates mutations in the new host. Ultimately, there is no single version of the disease present in the model, but rather an infinite number of them, each slightly different from the other.\hypertarget{md__r_e_a_d_m_e_old_autotoc_md50}{}\doxysection{Network dynamics}\label{md__r_e_a_d_m_e_old_autotoc_md50}
We can assume that the Population is organized in fully connected blocks for the first version of the model. Block sizes and the number of connections between blocks are Poisson random variables. Individuals interact with all the members of their blocks, and bridging individuals allow the disease to move across blocks.\hypertarget{md__r_e_a_d_m_e_old_autotoc_md51}{}\doxysection{Contagion dynamics}\label{md__r_e_a_d_m_e_old_autotoc_md51}
The transmission of the disease will be governed by the number of vaccinated, infected, and recovered within each block. Transmission between blocks will be treated in the same way, although individuals bridging the block will only interact with others within the block and their direct connections across the blocks.\hypertarget{md__r_e_a_d_m_e_old_autotoc_md52}{}\doxysection{Time dynamics}\label{md__r_e_a_d_m_e_old_autotoc_md52}
Time dynamics has two components, how biology evolves and how agents react.

The model develops as a continuous-\/time Markov process. Each block of individuals takes action at rates {\ttfamily L(i$\vert$N(i))} function of the local number of infections. This way, if ~\newline
\hypertarget{md__r_e_a_d_m_e_old_autotoc_md53}{}\doxysection{Updating agent\textquotesingle{}s status}\label{md__r_e_a_d_m_e_old_autotoc_md53}
Like most other components, updating agents\textquotesingle{} states can be personalized. A naive approach allows agents to get infected with a single virus or stay as-\/is. The probability of this event is conditional on acquiring at most one virus. Since these are independent events, the conditional probability is computed as follows\+:


\begin{DoxyCode}{0}
\DoxyCodeLine{P(Variant k|at most 1) = P(at most 1| Variant k) * P(Variant k)/P(at most 1)}
\DoxyCodeLine{                       = P(only variant k)/P(variant k) * P(Variant k)/P(at most 1)}
\DoxyCodeLine{                       = P(only variant k)/P(at most 1)}

\end{DoxyCode}


Where


\begin{DoxyCode}{0}
\DoxyCodeLine{P(only variant k) = P(k) * Prod(m!=v) (1 -\/ P(m))}
\DoxyCodeLine{P(at most 1) = P(None) + Sum(v in variants) P(v) * Prod(m != v) (1 -\/ P(m))}
\DoxyCodeLine{P(None) = Prod(v in variants) (1 -\/ P(v))}

\end{DoxyCode}


Furthermore, the (Variant, Person) pairs are treated independently.\hypertarget{md__r_e_a_d_m_e_old_autotoc_md54}{}\doxysubsection{Other parameters}\label{md__r_e_a_d_m_e_old_autotoc_md54}

\begin{DoxyItemize}
\item Who did you get the infection from.
\item Omicron is 1.\+5 more infectious than delta.
\item Surveillance\+:
\begin{DoxyItemize}
\item Pull people to be tested at random.
\item Or at symptoms.
\item A mix of the two.
\end{DoxyItemize}
\item Define a class for passing extra functions and datasets, for example, testing surveillance.
\item Exposed people become infectious after k days.
\item \mbox{\hyperlink{class_network}{Network}} changesthe can be a function of an ERGM. Apply K steps throughout time.
\item Add progress bar. 
\end{DoxyItemize}